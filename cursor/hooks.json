{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "command": "jq -r '.file_path' | { read -r f; case \"$f\" in *.md|*.yaml|*.yml|*.json|*.toml) oxfmt --write \"$f\" ;; esac; } 2>/dev/null || true"
      }
    ],
    "stop": [
      {
        "command": "./hooks/stop.sh"
      }
    ]
  }
}
