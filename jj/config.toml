"$schema" = "https://jj-vcs.github.io/jj/latest/config-schema.json"

[user]
name = "Oscar Söderlund"
email = "odsod@noreply.users.github.com"

[templates]
git_push_bookmark = '"odsod/push-" ++ change_id.short()'

[aliases]
rebase-all = ["rebase", "-s", "(::trunk())+ & mutable()", "-d", "trunk()"]
pr = ["util", "exec", "--", "bash", "-c", "jj git push -c @ && gh pr create --head \"odsod/push-$(jj log --no-graph -r @ -T 'change_id.short()')\" --fill"]

[ui]
editor = ["nvim"]
default-command = ["log", "--reversed"]
