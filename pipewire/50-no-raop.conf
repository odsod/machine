# Disable AirPlay/RAOP sink auto-discovery (Sonos Amps kept grabbing audio
# routing, e.g. herdr's notification bell). Re-enable by deleting this file.
context.properties = {
    module.raop = false
}
